# Dépendances (NE JAMAIS ENVOYER ÇA, c'est trop lourd)
node_modules/
package-lock.json # Optionnel : certains préfèrent l'envoyer, d'autres non. Si tu débutes, tu peux le laisser hors du gitignore (donc ne mets pas cette ligne)

# Next.js build output
.next/
out/
build/
dist/

# Fichiers d'environnement (TRÈS IMPORTANT : contient tes mots de passe/clés API)
.env
.env*.local
.env.production

# Logs et debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Système (Mac/Windows/Linux)
.DS_Store
Thumbs.db
